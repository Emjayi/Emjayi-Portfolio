import Link from "next/link";
import { useEffect, useRef } from "react";

export default function OpeningLink() {
    const ref = useRef(null)
    useEffect((
        
    ))
    return (
        <Link href={"/projects"} ref={ref}>


        </Link>
    );
}